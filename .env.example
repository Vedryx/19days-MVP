# Required for /api/callback to persist Pulse callback requests.
# Without these, the API returns 500 and form submits are silently dropped.
# Mirror these values into the Vercel project env (Production + Preview).
MONGODB_URI=
MONGODB_DB=vedryx
